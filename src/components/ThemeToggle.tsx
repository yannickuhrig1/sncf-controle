// ThemeToggle is no longer used — theme switching is handled directly in Settings.tsx
// This file is kept as a no-op export to avoid import errors.
export function ThemeToggle() {
  return null;
}
